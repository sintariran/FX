取引用の分岐探索
分岐探索出力を取引履歴に登録
取引履歴実行
損益実行

数式を分割して、関数を抜き出す処理に４～６時間
要因は、関数や使用対象パッケージによる絞り込みをしても、母数が多い42000件。
該当条件のルートIDを絞り込む処理（フィルタリング関数）

エクセル関数上に前提をもって範囲指定する箇所がある場合に、

４～６時間（リストの出力を行う）
自分自身が素材
素材の区分の重複
範囲してする箇所がパッケージ場できていない
前提はあるが、範囲指定
範囲指定する際にその範囲指定する目的の特定
見つからないがほとんどになっている　⇒　新規作成が多い

エクセルA列（関数行番号）
エクセルD列（ルートID）
全部1,5,15,30,60,240が成立しているルートID
INDEXは、どのルートIDの値を取得するかが、連番できまる（タイプSL）
　　　　連番は、MATCH関数の出力を使用する
MATCHは、連番を出力する。出力対象の連番は、素材1の値と範囲指定ないのルートIDの出力値との合致確認結果に基づく。
※MATCHの素材に関しては、
フォーマットでパッケージ化すればよいか。

●MATCHのフォーマット
※２ステップ構成
－ステップ１（範囲指定のルートIDと条件1のルートIDの一致判定）
タイプX2
条件1と範囲指定のルートIDの一致を確認（出力3のルートIDを取得）

－ステップ２（一致判定結果に基づく出力値の選定）
タイプSL
行数は範囲指定の行数分用意
出力値は範囲指定の行番号（1～n）191^1～191^n
条件はタイプX2の出力値が3のルートIDを各範囲指定の行数別に指定
※ルートIDは出力値（1～n）毎に分ける

●INDEXのフォーマット
タイプSL
行数は範囲指定の行数分用意
出力値は範囲指定の行ごとのルートIDの出力値
条件は行／列の固定値でない側の出力値（1～n）に応じたルートIDを行番号に合わせて設定

●sumのフォーマット
※使用されている箇所は加速の合算処理のみ（それ以外は信号成立を簡易に判断するためのもののため、エクセル関数の修正）
タイプZの1（複数回実行）
素材の指定範囲から素材を順番に積算する
範囲指定の素材数分段階的な合算値の値が出力される

●modのフォーマット
タイプZ(8)
引数1を条件1に設定
引数2を条件2に設定

●O1参照（O$1)参照のフォーマット
タイプND
191^1-3150で作成済み

●minuteのフォーマット
タイプMN
引数1を条件1に設定

●ROUNDDOWNのフォーマット
２ステップ
－ステップ1（材料から0.49999を引く）
タイプZ(2)
条件1に引数
条件2に191^0.49999

ーステップ2（四捨五入処理）
タイプRO
条件1にステップ1の結果

●countaのフォーマット
タイプU
出力値は範囲指定の要素数（固定値として出力可）

●min/maxのフォーマット
※パッケージ機能としての未実装の内容だったため、イエに直接組み込んでいる（表示器用）

●averageのフォーマット※リアルで処理できないため、対象から外す
2ステップ
－ステップ１（データ数の取得）
タイプU
出力値は、範囲指定のデータ数の取得（終了と開始の列／行の差分数で多い方）
※offset関数が使用されている場合、offset関数の4つ目の素材の値を参照

－ステップ２（合計値の取得及びデータ数での除算）
タイプZ
出力値は、範囲

●offset（averageでのみ使用されていたため、対象から一緒に外す）
※範囲指定のみの機能のため、パッケージ化しない（使用箇所も1か所のみ）
範囲指定処理されていることから情報を取得する

