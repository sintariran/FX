
M:前足
I:今足
J:実勢
H:平均

・MHIHとMJIHがそろったらその方向 55.7%/53.5%
・MHIHとMJIHがそろていない場合
 -MMHMHとMMJMHの方向 56.1%/56.0%
 -MJIJとMMHMH/MMJMHの方向が合致しない場合、MHの確定方向を確認する 55.8%/55.8%
※MHIHとMJIHの転換足でMMHMHとMMJMHが逆を向いていて、MHの確定方向も逆の場合、転換しない 56.0%/56.0%
　その次の足でMMHMHやMHが転換したら、それをトリガーとして判断
・MJIHが転換した際に、ぜんもみの時は、全時間そろうまでカウンター
・MJIHが転換した際に、Mもみで2x5成立している場合、カウンター
・MHIHとMJIHが転換していない状態でそれ以外が返してきている場合に対しての早期判定が必要
・MJIHが転換した際に、MMJMJがMMJMH,MMHMHと逆を向いている場合、その足だけは判断を待って、次の足でMMJMJが逆向きのまま（または、ピンバー）の場合、転換させない。


10/7 16:51 ピンバー
10/9 11:45 ピンバー

もみは、もみになっていない小さい時間足側ともみのカウンターの方向が合致していること

●追加パッケージの仕様
・ＭＯＤ関数
 type_z
・1,2で構成されるパッケージを素材として、そのステートが切り替わった時刻を起点として、1から順番に軸時間毎にカウントアップする
 type_co (count)
・素材として指定した数字を基に値を計算する（5が指定されたら、5+4+3+2+1の値を出力する）
 type_sd (sum_down)
・対象の時刻を分換算に置き換える関数
 type_mn (minute)
　※モード1の場合、現在時刻から算出。モード2の場合、条件1に記載するパッケージが持っている時刻データから算出。
・素材1で指定されたパッケージで区間の切替りを1,2で指定し、素材2で積算対象の数字を指定する。
　素材1と素材2のパッケージに基づいて、区間毎の値の和算をする
 type_sg (sum_group)
・隣接線の価格（基準線との予知評価用）
・隣接線の周期（周期特定用）
 type_nl（near_line モードで指定 1が直近の上価格、2が直近の下価格、3が直近の上周期、4が直近の下周期）
　※周期はターゲットの周期コードの数字を返す
・周期特定（設定ファイル上で指定したトリガーが成立したタイミングで最終成立周期を保持して、常時出力する）
 type_ss (specified_shuki)

+2(pinbar関連）
・理論値関数
Type_I(ideal)
素材として指定された上下の区間において、最小値と最大値を算出し、それぞれの足で方向を出す
・区間平均関数
Type_AS(average_section)
素材1に平均したい区間用のPKGを指定し、素材2に平均したい数値(価格)のPKGを指定する
出力に1を指定した場合は現区間の平均を出し、2を指定した場合は1つ前区間の平均を出す